{{#> _Layout title="Anime y Series Favoritas" active="animeseries"}}
<h1 class="text-2xl font-bold mb-6">🎬 Anime / Series Favoritas</h1>
<div class="grid grid-cols-1 md:grid-cols-3 gap-6">
    {{#each Items}}
    <div class="bg-white rounded shadow p-4">
        <img src="{{CaratulaPath}}" alt="{{Titulo}}" class="w-full rounded mb-2">
        <h3 class="font-semibold text-lg">{{Titulo}}</h3>
        <p class="text-gray-600 text-sm">{{Descripcion}}</p>
        <div class="mt-2">
            <iframe class="w-full h-48 rounded" src="{{TrailerUrl}}" frameborder="0" allowfullscreen></iframe>
        </div>
    </div>
    {{/each}}
</div>
{{/_Layout}}
