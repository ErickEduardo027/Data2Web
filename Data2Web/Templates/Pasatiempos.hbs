<section class="max-w-5xl mx-auto">
    <h2 class="text-3xl font-bold mb-6">Mis pasatiempos 🎨</h2>
    <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-6 gallery">
        {{#each Items}}
        <div class="bg-white rounded-xl shadow-lg overflow-hidden hover:shadow-xl transition">
            <img src="{{ImagenUrl}}" alt="{{Titulo}}" class="w-full h-48 object-cover">
            <div class="p-4">
                <h3 class="font-bold text-lg mb-2">{{Titulo}}</h3>
                <p class="text-gray-600 text-sm">{{Descripcion}}</p>
            </div>
        </div>
        {{/each}}
    </div>
</section>
