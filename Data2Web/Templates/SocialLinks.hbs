<section class="text-center">
    <h2 class="text-3xl font-bold mb-8">Conéctate conmigo 🌐</h2>

    <div class="grid gap-6 md:grid-cols-3">
        {{#each Items}}
        <a href="{{Url}}" target="_blank" class="group bg-white shadow-md rounded-xl p-6 flex flex-col items-center hover:shadow-xl transition">

            <!-- Icono SVG dinámico -->
            <div class="text-blue-600 mb-3 w-3 h-3">
                {{{IconoSvg}}}
            </div>

            <h3 class="text-lg font-semibold mb-1">{{RedSocial}}</h3>
            <p class="text-sm text-gray-500 break-all">{{Url}}</p>
        </a>
        {{/each}}
    </div>
</section>

