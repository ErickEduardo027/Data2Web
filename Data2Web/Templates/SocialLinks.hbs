{{#> _Layout title="Redes Sociales" active="sociallinks"}}
<h1 class="text-2xl font-bold mb-6">🌐 Redes Sociales</h1>
<div class="flex flex-wrap gap-6">
    {{#each Items}}
    <a href="{{Url}}" target="_blank" class="flex items-center space-x-2 bg-gray-100 px-4 py-2 rounded shadow hover:bg-gray-200">
        {{{IconoSvg}}}
        <span class="font-semibold">{{RedSocial}}</span>
    </a>
    {{/each}}
</div>
{{/_Layout}}
